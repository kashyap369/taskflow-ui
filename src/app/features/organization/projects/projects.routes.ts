import { Routes } from '@angular/router';

/**
 * Projects bounded context — scaffold.
 * Add pages under presentation/pages/ and wire this into the organization
 * route branch (children of OrganizationLayout) when the first page exists.
 */
export const PROJECTS_ROUTES: Routes = [];
